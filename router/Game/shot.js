const router = require('express').Router()


router.post('/:id/shots', async (req, res, next) => {
   
    // post

})


router.delete('/:id/shots/previous', async (req, res, next) => {
  // delete
})

module.exports = router
